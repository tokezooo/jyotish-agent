.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
.pi/secrets.env
node_modules/
# Swiss ephemeris data is downloaded by scripts/install_ephemeris.py, licensed
# separately, and not redistributed.
*.se1
# The Swiss golden fixture is generated locally (depends on .se1 + pyswisseph
# version); the Swiss path is verified by a tolerance-based parity test instead.
tests/fixtures/golden_chennai_1990_swiss.json
tests/fixtures/golden_chennai_1990_swiss_modules.json

# uv.lock IS committed: PyJHora declares no runtime deps, so the lock is the only
# record of the headless calculation dependency set. Do not ignore it.
.gstack/
.worktrees/
.superpowers/
# Copyrighted/licensed doctrine source binaries are versioned only through Git LFS in
# this private repository. This is storage, not rights clearance: never make
# the repository public, create a public fork/mirror/archive, or grant access
# to anyone other than its owner. Add any new binary source format to
# .gitattributes before staging it.
#
# The source root is intentionally trackable; the following non-source and
# sensitive artifacts must remain excluded even beneath private_sources/.
private_sources/**/.env
private_sources/**/.env.*
private_sources/**/.venv/
private_sources/**/.git/
private_sources/**/credentials/
private_sources/**/credentials.*
private_sources/**/*credential*
private_sources/**/tokens/
private_sources/**/token.*
private_sources/**/*token*
private_sources/**/*.pem
private_sources/**/*.key
private_sources/**/id_rsa*
private_sources/**/profiles/
private_sources/**/*.sqlite
private_sources/**/*.sqlite3
private_sources/**/*.db
private_sources/**/*.log
private_sources/**/output/
private_sources/**/tmp/
private_sources/**/computed_facts.json
private_sources/**/natal_chart_detailed.md
private_sources/**/server_response.json
private_sources/**/.DS_Store
private_sources/**/._*
private_sources/**/Thumbs.db
private_sources/**/desktop.ini
# Opt-in user/reviewer session records remain local. Only reviewed aggregate
# metrics may be copied into tracked doctrine evidence.
private_evidence/
