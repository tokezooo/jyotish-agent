# K. N. Rao OCR-reviewed fragment slice

## Objective

Remove the bounded K. N. Rao `ocr_review_pending` blocker without admitting doctrine or activating an overlay. Fix the real Poppler renderer path, review a minimal rule-bearing page set against the scan, then publish only content hashes, coordinates, comparison metrics, quarantined bindings, and release blockers.

## Safety contract

- Copyrighted source bytes and extracted text remain local and untracked.
- Tracked fragments use `excerpt_permission=none`, no excerpt, and `admission_status=quarantined`.
- OCR/visual review is technical evidence quality review, not specialist doctrine approval.
- Every K. N. Rao binding remains `quarantined_conflict`; no compiled rule or activation state changes.
- The frozen Jaimini core, baseline inventory, and Sanjay Rath commitments remain unchanged.
- `full_jaimini_v1` remains `blocked_sources`, `available=false`.
- Preserve unrelated untracked files, including `tmp/` and the governed-hybrid plan.

## Reviewed scope

Active source: `jaimini_kn_rao_chara_dasha_vani_scan_2010`, exact manifest SHA-256 already verified.

Selected PDF / printed pages:

- 32 / 33 — major-period progression;
- 35 / 36 — sub-period ordering;
- 39 / 40 and 40 / 41 — calculation and duration method;
- 43 / 44 — co-lord resolution;
- 44 / 45 — duration modifiers and explicit school differences.

Each page has been visually compared with the embedded OCR layer and independently re-OCRed with Tesseract. The tracked audit records only hashes, token counts, similarity/confidence metrics, and dispositions.

## Tasks

### 1. Repair real OCR rendering

RED: prove that `PdftoppmRenderer` must read the generated single-page image instead of assuming the output prefix `-` means stdout.

GREEN: render inside an isolated temporary directory, return image bytes, clean temporary artifacts, and preserve typed/path-safe failures.

### 2. Generalize the fail-closed overlay ledger

RED: a K. N. Rao ledger is rejected by the Sanjay-only literals and source-set checks.

GREEN: allow exactly two ledger identities (`sanjay_rath`, `kn_rao_practical`) with exact per-overlay source sets, uniform school identity, empty unresolved-source lists, and the existing unavailable/quarantine contract. No cross-school ledger is valid.

### 3. Bind privacy-safe K. N. Rao evidence

Generate six text-free SourceFragments from repository normalization and the verified embedded OCR layer. Bind them to `chara_dasha.progression`, `chara_dasha.antardasha`, `chara_dasha.duration`, and `co_lords.resolution`, all as explicit conflicts.

Add a separate K. N. Rao ledger and a privacy-safe OCR review audit. Remove the now-reviewed source from the Sanjay ledger unresolved queue without changing either overlay availability.

### 4. Synchronize release evidence

Update overlay missing reasons and release/audit projections to state that the bounded OCR slice is reviewed and hash-bound but still quarantined, uncompiled, and missing specialist review. Keep Subodhini, compiled-profile, hand-worked, and deep conversational gates open.

### 5. Verify and integrate

- renderer and ingestion regressions;
- both overlay ledgers and adversarial source/school substitutions;
- private replay for the six reviewed pages when local bytes exist;
- canonical audit hashes;
- full Python, Ruff, Bun, TypeScript, and tracked-private-source scans;
- independent review before merge;
- merged PR and Linear evidence update.
