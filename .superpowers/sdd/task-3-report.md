# Task 3 report — Jaimini Core geometry and timing

## Status

Implemented the complete requested pure domain in `src/jyotish_agent/jaimini.py` without MCP,
rendering, API, ResearchRun, or locked-kernel interface changes. The implementation captures real
natal D1/D9 data only through `_run_engine_session(..., domain_callback=...)`.

## TDD evidence

### Initial RED

Command:

```text
uv run pytest -q tests/test_jaimini_core.py
```

Observed output before production code existed:

```text
E   ModuleNotFoundError: No module named 'jyotish_agent.jaimini'
1 error in 0.07s
```

This failure was caused by the missing feature module, not a typo or a pre-existing failure.

### Additional RED cycles

The locked-snapshot compatibility test was added before tuple-snapshot support and failed at
`chart[body]` with `TypeError: tuple indices must be integers or slices, not str`. After adding
support for the immutable callback positions, the 8-karaka Rahu case was added and observed failing
at `_SEVEN_PLANETS.index("Rahu")`. Both then passed after minimal implementation changes.

The runtime closed-scheme test was added before validation and observed `scheme=9` being treated as
the 8-karaka branch (failure reported missing Rahu instead of rejecting the scheme). The production
function now rejects all values except frozen schemes 7 and 8.

### GREEN

Focused command and output:

```text
uv run pytest -q tests/test_jaimini_core.py
17 passed in 0.95s
```

Relevant contract/kernel/golden/concurrency command and output:

```text
uv run pytest -q tests/test_jaimini_core.py tests/test_task1_jaimini_contracts.py tests/test_locked_engine_session.py tests/test_pyjhora_facade_golden.py
65 passed, 2 skipped in 26.37s
```

Full suite command and output:

```text
uv run pytest -q
442 passed, 2 skipped in 106.15s
```

Both skips are the pre-existing Swiss-only golden checks; Moshier is installed and passed.

## Algorithms and frozen-profile mapping

| Area | Deterministic implementation | Frozen choice / boundary |
|---|---|---|
| Chara karakas | Ranks Sun through Saturn for 7; adds Rahu for 8; converts sign degrees to one-arcsecond scores | Schemes 7/8; Rahu score `30° - degree`; exact ties raise `ExactKarakaTieError`; differences up to 60 arcseconds flag without reordering |
| Rasi drishti | Movable -> non-adjacent fixed; fixed -> non-adjacent movable; dual -> other dual signs | Pure zero-based sign geometry, symmetric invariant tested |
| Arudha | Inclusive sign count applied from house to lord and again from lord | Same/seventh projection moves to the tenth from the source house; A1-A12 plus AL=A1 and UL=A12 |
| Co-lords | Lexicographic maximum by rashi duration, then degree, then frozen fixed order | `greater_rashi_duration_then_degree_then_fixed_order`; Mars/Ketu and Saturn/Rahu are the only accepted pairs |
| Argala | 2/4/11 primary and 5 secondary contributions | Virodhargala pairs 12/10/3/9 respectively; returns contributors and obstructors without interpretation |
| Svamsa / karakamsa | D9 lagna sign / D9 sign occupied by AK | Accepts both hand-auditable mappings and actual immutable locked-session position tuples, including Rahu AK |
| Selected special lagnas | BL/HL/GL advance from solar longitude at one sign per 120/60/24 elapsed minutes | Pure modulo-360 geometry; no engine prediction API |
| Chara Dasha seed/progression | Male starts at lagna; female at seventh; odd-numbered sign seed progresses forward, even reverse | Required binary gender; odd-forward/even-reverse |
| Maha duration | Directional exclusive distance from period sign to resolved lord, minimum one | Equivalent to frozen inclusive-count-minus-one-minimum-one rule |
| Antardasha | Twelve equal microsecond-rounded partitions in the maha sign's direction, beginning with the next sign | Same direction from next sign |
| Time | Decimal arithmetic over 365.2425 days/year; ROUND_HALF_EVEN at microsecond; contiguous periods | Exact `[start,end)` membership and boundary tests |
| Birth confidence | Exact evaluates once; approximate evaluates earliest through latest inclusively at five-minute steps, max 120 minutes; unknown rejects | 1/3/25 sample boundaries and stable/unstable fact classification tested |
| Natal capture | One `_run_engine_session` call per permitted sample; callback returns immutable snapshot containing D1/D9 | No second engine path, no raw mutable chart access, no locked interface change |

## Independent fixture evidence

`tests/fixtures/jaimini_core_hand_checked_v1.json` is static and explicitly states that it is not
generated by `jyotish_agent.jaimini`. Expectations are small enough to audit by hand: degree ranks,
zero-based signs, and integer sign-distance tables. Tests only read the JSON; production code never
imports it.

Two expectation groups were corrected during the first GREEN attempt from independent arithmetic,
not copied from production output:

| Case | Initially written | Hand derivation | Correct expectation |
|---|---|---|---|
| Arudha, Gemini house (2), lord Virgo (5) | 8 | Inclusive distance is four signs; projecting four from Virgo gives Sagittarius (8), which is seventh from Gemini, so the frozen exception moves to the tenth from Gemini | Pisces (11) |
| Chara duration, Pisces (11) to lord Gemini (2) | 10 years | Pisces is an even-numbered sign and counts in reverse: Aquarius 1, Capricorn 2, Sagittarius 3, Scorpio 4, Libra 5, Virgo 6, Leo 7, Cancer 8, Gemini 9 | 9 years |

The same explicit rules were then applied across the complete 12-sign fixture arrays. The exact
timeline fixture is independently checked as one year after `2000-01-01T00:00:00Z` =
`2000-12-31T05:49:12Z`, and one twelfth = `2000-01-31T10:29:06Z`.

The packaged Phase-0 adjudication fixtures remain untouched and pending, as required by their
frozen source-review gate; this task does not falsely claim qualified human/source approval.

## Files

- `src/jyotish_agent/jaimini.py` — focused pure geometry/timing and locked snapshot capture.
- `tests/test_jaimini_core.py` — example, property, boundary, confidence, callback, and real-kernel tests.
- `tests/fixtures/jaimini_core_hand_checked_v1.json` — static hand-counted expectations.
- `.superpowers/sdd/task-3-report.md` — this report (force-added because `.superpowers/` is ignored).

## Self-review

- No existing production file or public schema was modified.
- No PyJHora Jaimini/prediction helper is called; runtime doctrine is independent.
- All returned collections that define order are deterministic; signs are validated as 0..11.
- Dasha maha and antar partitions are gapless, non-overlapping, positive, and exactly bounded.
- Actual D1/D9 callback compatibility is exercised against the installed kernel, not only a mock.
- `python -m compileall`, `git diff --check`, focused tests, relevant regression tests, and full suite pass.

## Concerns / explicit limits

1. The frozen JSON profile explicitly serializes karaka, arudha, co-lord, Chara Dasha, and time
   choices, but does not serialize separate choice fields for rasi drishti, argala, svamsa,
   karakamsa, or BL/HL/GL. Those requested items use the conventional deterministic definitions
   stated in the table and tests; a future profile version should encode them if multiple schools
   are to be supported.
2. The frozen Jaimini input model requires binary gender semantically but has no gender field.
   Therefore the pure `chara_dasha` function requires gender explicitly; this task correctly avoids
   changing the public model/API, but a later facade task must resolve that contract gap.
3. Static fixture expectations are implementation-independent and hand-auditable, but are not
   claimed as qualified human/source-adjudicated. The packaged source map and adjudication fixtures
   intentionally remain `pending`; interpretation must continue to fail closed.

## Review-fix addendum

Commit `10cb855` was reviewed and the requested findings were corrected with another strict TDD
cycle. This addendum supersedes concerns 1 and 2 above; concern 3 remains an intentional fail-closed
source-admission state.

### Review RED evidence

The new profile-mapping tests were run before production changes:

```text
uv run pytest -q tests/test_jaimini_core.py tests/test_task1_jaimini_contracts.py -x
E   TypeError: rasi_drishti() got an unexpected keyword argument 'profile'
1 failed, 2 passed in 0.22s
```

That RED proved runtime doctrine was not yet profile-bound. The same test-first patch added checks
for missing Dasha gender, forbidden geometry-only gender, non-five-minute range width, and argala
empty/equal/unequal count outcomes before implementing those behaviors.

### Frozen doctrine migration

`JaiminiRuleProfile` and `jaimini_core_v1.json` now explicitly and immutably freeze:

- modal rasi drishti branches;
- argala houses, all four virodhargala pairs, count comparison, and the four result statuses;
- svamsa as D9 lagna and karakamsa as D9 Atmakaraka;
- BL/HL/GL inclusion, sunrise-solar anchor, elapsed-minute input, 120/60/24-minute-per-sign rates,
  and modulo-360 wrapping.

The affected runtime functions accept a validated `JaiminiRuleProfile`; omitting it loads the
packaged profile. They read their houses, pairs, statuses, included lagnas, and rates from that
profile and fail on unsupported variants rather than executing untracked doctrine.

Checksum migration was performed in dependency order:

```text
jaimini_core_v1.json         8a97c43e8438351ae73a59fed731cf5e48ef3c25c347ddf3570dea5818696b58
jaimini_core_v1_sources.json 5e494796568722aff34fdc6a9fc43634809928d5afb6c1771574119c04ea3095
```

The source map was bound to the new profile hash and gained pending mappings for rasi drishti,
argala geometry/count obstruction, svamsa, karakamsa, and selected special-lagna rates. All five
package adjudication fixtures were then migrated to both new hashes. Their expected scenarios and
manual fixture arithmetic were not regenerated or changed. Source review remains `pending`, every
new mapping remains `pending`, and `interpretation_status` remains `unavailable`.

### Input and sampling corrections

The additive `JaiminiInput` now has a strict optional binary `gender` field with cross-field rules:

- `core_with_chara_dasha` requires `female` or `male`;
- geometry-only `core` forbids gender because it is unused.

No existing `BirthProfile` or Research model changed. Approximate inputs now reject widths not
divisible by five minutes. Both `sensitivity_sweep` and locked-session `capture_birth_snapshots`
call the same `_sample_instants` generator, so `10:00..10:10` is exactly three inclusive samples
and `10:00..10:12` is rejected consistently.

Argala results now expose an explicit status determined by frozen count comparison:

- no contributors: `absent`;
- contributors and no obstructors: `unobstructed`;
- fewer obstructors than contributors: `partial`;
- equal or more obstructors: `obstructed`.

### Review GREEN evidence

```text
uv run pytest -q tests/test_jaimini_core.py tests/test_task1_jaimini_contracts.py
42 passed in 1.61s

uv run pytest -q tests/test_jaimini_core.py tests/test_task1_jaimini_contracts.py tests/test_locked_engine_session.py tests/test_pyjhora_facade_golden.py
67 passed, 2 skipped in 26.06s

uv run pytest -q
444 passed, 2 skipped in 106.09s
```

The two skips remain the pre-existing Swiss-ephemeris-only goldens. `git diff --check` is clean.

### Remaining concern

Qualified human/source adjudication is still pending by design. Calculation choices are now fully
tracked and executable, but interpretive Jaimini output must remain unavailable until that separate
Phase-0 gate is honestly approved.
